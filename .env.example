# Base URL of the main marketing site (cross-site) links point to.
# No trailing slash. Links are built as `${NEXT_PUBLIC_SITE_URL}${path}`, e.g. `/contact-us`.
NEXT_PUBLIC_SITE_URL=""

# Emailing (get-help form submissions)
SMTP_HOST=""
SMTP_PORT=""
SMTP_USER=""
SMTP_PASS=""
EMAIL_TO=""
MAIL_SECURE="true"

# Recaptcha
NEXT_PUBLIC_RECAPTCHA_PUBLIC=""
NEXT_RECAPTCHA_SECRET=""
